# Linter Configs

This directory contains shared linter configuration files used by both:

- Super-Linter in CI: [`super-linter job`](../workflows/lint.yml)
- Local `prek` hooks: [`prek config`](../../.pre-commit-config.yaml)

Keeping these configs together helps keep local checks and CI checks aligned.
